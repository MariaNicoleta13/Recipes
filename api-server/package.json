{
  "name": "api-server",
  "version": "1.0.0",
  "description": "json server to keep data",
  "main": "index.js",
  "scripts": {
    "start": "json-server -p 3001 -w db.json"
  },
  "author": "Mary",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
